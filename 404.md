---
title: 页面未找到
description: 你访问的页面不存在
---

<div class="not-found">
  <div class="not-found-code">404</div>
  <div class="not-found-divider"></div>
  <h1>页面迷失在星空中</h1>
  <p>你访问的页面可能已被删除、更名或暂时不可用</p>
  <div class="not-found-actions">
    <a href="/AstralLeap/" class="btn btn-primary">🏠 返回首页</a>
    <a href="/AstralLeap/blog/" class="btn btn-secondary">📝 浏览博客</a>
    <a href="/AstralLeap/projects/" class="btn btn-secondary">💼 查看项目</a>
  </div>
  <p class="not-found-hint" id="hint">如果你从本站链接跳转，将在 <span id="countdown">3</span> 秒后自动返回...</p>
</div>

<script>
(function() {
  // 倒计时自动返回
  var ref = document.referrer;
  if (ref && ref.indexOf(location.hostname) !== -1) {
    var count = 3;
    var el = document.getElementById('countdown');
    var timer = setInterval(function() {
      count--;
      if (el) el.textContent = count;
      if (count <= 0) {
        clearInterval(timer);
        history.back();
      }
    }, 1000);
  } else {
    var hint = document.getElementById('hint');
    if (hint) hint.style.display = 'none';
  }
})();
</script>

<style>
html, body { margin: 0; padding: 0; height: 100%; }
.not-found {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  min-height: 100vh;
  text-align: center;
  padding: 2rem;
  font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
  background: radial-gradient(ellipse at center, rgba(139, 92, 246, 0.05) 0%, transparent 70%);
}
.not-found-code {
  font-size: 8rem;
  font-weight: 900;
  background: linear-gradient(135deg, #8b5cf6, #06b6d4);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  line-height: 1;
  letter-spacing: -0.05em;
}
.not-found-divider {
  width: 80px;
  height: 3px;
  background: linear-gradient(90deg, #8b5cf6, #06b6d4);
  border-radius: 2px;
  margin: 1.5rem 0;
}
.not-found h1 { font-size: 1.75rem; margin: 0 0 0.75rem 0; color: #333; }
.not-found p { color: #666; margin: 0 0 2rem 0; font-size: 1rem; }
.not-found-actions { display: flex; gap: 1rem; flex-wrap: wrap; justify-content: center; margin-bottom: 2rem; }
.btn {
  padding: 0.7rem 1.5rem;
  border-radius: 8px;
  text-decoration: none;
  font-weight: 500;
  font-size: 0.95rem;
  transition: all 0.2s ease;
}
.btn-primary {
  background: linear-gradient(135deg, #8b5cf6, #6366f1);
  color: white;
  box-shadow: 0 4px 12px rgba(139, 92, 246, 0.3);
}
.btn-primary:hover { transform: translateY(-2px); box-shadow: 0 6px 20px rgba(139, 92, 246, 0.4); }
.btn-secondary { background: #f5f5f5; color: #333; border: 1px solid #e5e5e5; }
.btn-secondary:hover { border-color: #8b5cf6; color: #8b5cf6; }
.not-found-hint { font-size: 0.85rem !important; color: #999 !important; }
.not-found-hint span { font-weight: 700; color: #8b5cf6; }
@media (max-width: 640px) {
  .not-found-code { font-size: 5rem; }
  .not-found h1 { font-size: 1.35rem; }
  .not-found-actions { flex-direction: column; }
  .btn { text-align: center; }
}
</style>
